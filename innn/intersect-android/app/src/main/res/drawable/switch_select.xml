<?xml version="1.0" encoding="utf-8"?><!-- 底层下滑条的样式选择器，可控制Switch在不同状态下，底下下滑条的颜色 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_checked="true">
        <shape android:shape="rectangle">

            <!-- 高度30   此处设置宽度无效-->
            <size android:height="20dp" />
            <size android:width="30dp"/>
            <!-- 圆角弧度 15 -->
            <corners android:radius="10dp" />
            <!-- 变化率  定义从左到右的颜色不变 -->
            <gradient android:endColor="@color/blackBackground" android:startColor="@color/blackBackground" />
        </shape>
    </item>

    <item >
        <shape android:shape="rectangle">

            <!-- 高度30   此处设置宽度无效-->
            <size android:height="20dp" />
            <size android:width="30dp"/>

            <!-- 圆角弧度 15 -->
            <corners android:radius="10dp" />
            <!-- 变化率  定义从左到右的颜色不变 -->
            <gradient android:endColor="@color/colorGray" android:startColor="@color/colorGray" />
        </shape>
    </item>


</selector>